services:
  - type: web
    name: shl-assessment-system
    env: python
    buildCommand: pip install -r requirements.txt && python setup.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
